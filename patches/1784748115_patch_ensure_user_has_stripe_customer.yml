id: patch-ensure-user-has-customer
language: go

rule:
  pattern: |
    func ($RECV) EnsureUserHasCustomer($$$PARAMS) ($$$RETURNS) {
      $$$BODY
    }

fix: |
  func ($RECV) EnsureUserHasCustomer($$$PARAMS) ($$$RETURNS) {
      // Stripe integration disabled: no customer to ensure, no-op.
      return nil
  }
